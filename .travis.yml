language: python

python:
  - "2.7"
  
install:
  - pip install

script:
  # Install twine
  - pip install twine
  # Create a source distribution
  - python setup.py sdist
  # Upload to pypi
  - twine upload dist/*
